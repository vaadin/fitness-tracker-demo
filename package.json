{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.9.1",
    "del": "^2.0.2",
    "gulp": "^3.9.0",
    "gulp-rename": "^1.2.2",
    "merge-stream": "^1.0.0",
    "run-sequence": "^1.1.4"
  },
  "dependencies": {
    "polybuild": "^1.0.5"
  }
}
